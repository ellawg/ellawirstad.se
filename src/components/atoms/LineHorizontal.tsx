import React, { useState } from "react";
import Line from "../../assets/line-horizontal.svg";

const LineHorizontal = ({}) => {
  return (
    <div className="flex my-4 items-center">
      <img className="w-1/4" src={Line} />
    </div>
  );
};

export default LineHorizontal;
